# Vercel frontend environment variables (Project Settings → Environment Variables)

# Google AI Studio key — server-side only, used by /api/employee. Never exposed to the browser.
GEMINI_API_KEY=

# Public URL of the Render backend, e.g. https://rudra-backend.onrender.com (no trailing slash).
NEXT_PUBLIC_BACKEND_URL=
